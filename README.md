# OpenAI Assistant API - Function Calling Demo
This code is experimental and not intended for production use.
Use at your own risk.

"""
To install and run the app:
1. Install the required packages listed in requirements.txt using pip.
2. Set the FLASK_APP environment variable to hello.py.
3. Run the command 'flask run' in the terminal.

Example:
$ pip install -r requirements.txt
$ export FLASK_APP=app.py
$ flask run
"""